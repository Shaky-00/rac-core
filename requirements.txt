# Pinned ranges for anonymous artifact evaluation (see pyproject.toml for package metadata).
# Install from repo root:  python3 -m pip install -r requirements.txt && python3 -m pip install -e .

pydantic>=2.7,<3.0
mcp>=1.0,<2.0
PyYAML>=6.0,<7.0
pytest>=8.0,<9.0

# Optional: development / paper figure generation (not required for core checker or TraceBench replay)
# matplotlib>=3.8,<4
